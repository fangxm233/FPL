class Program
{
	//Program program;
	//static int i;
	static void Main()
	{
		//int a = 666 + 555 * 1 / 666 * (555 + 1);
		//int b = a + 1 + pp(555, new Program().i);
		//program.i = 0;
		int a;
		for (int i = 0; i <10000000; i = i + 1)
		{
			a = a + 1;
		}
	}

	static int pp(int a, int ssss)
	{
		return 0;
	}
}

/*
目前支持:
for
if
while
do while
continue
break
return
new
构造函数
强类型声明
强类型运算
class类型
函数
支持参数，返回值
多文件（那个System文件是声明基本类型，别改啊）
静态变量和函数

运算符:
+,-,*,/,.

单行注释和多行注释

将要支持:
函数重载
继承
函数重写
*/