class Program
{
	Program program;
	int i;
	static void Main()
	{
		int a = 666 + 555 * 1 / 666 * (555 + 1);
		int b = a + 1 + pp(555, new Program().i);
		program.i = 0;
	}

	static int pp(int a, int ssss)
	{
		return 0;
	}
}

/*
目前支持:
for
if
while
do while
continue
break
return
new
构造函数
强类型声明
强类型运算
class类型
函数
支持参数，返回值
多文件（那个System文件是声明基本类型，别改啊）

支持的运算符:
+,-,*,/,.

只有普通表达式才支持括号QAQ （说告诉我怎么区分bool表达式和普通表达式的括号啊）

支持单行注释和多行注释

静态变量和函数支持编译，但是不支持执行，写别啊
*/